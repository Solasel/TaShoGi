/* Implements an abstraction for the shogi board
   and various related helper functions. */

#include <board.h>

/* Enumerates all legal moves on board b into *rv. */
int enumerate_moves(struct board *b, struct move **rv)
{
	/* TODO */
	return NOT_YET_IMP;
}

